#!/bin/bash


#!@Let This run even if you already installed docker and other files@!


#To run the docker container you need to install docker
sudo apt install docker
#To auto start the docker container we need to install docker compose
sudo apt install docker-compose

echo "Installation complete you can continue with **make execute***"
